import { Typography } from "@mui/material";

const LandingComponent: React.FC = () => {
    return (
        <Typography variant="h1">TEMPORARY PLACEHOLDER</Typography>
    );
};

export default LandingComponent;